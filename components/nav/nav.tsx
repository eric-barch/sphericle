import { ClientSideNav } from "./client-side-nav";
import { SignInButton } from "./sign-in-button";

const Nav = () => {
  return (
    <ClientSideNav>
      <SignInButton />
    </ClientSideNav>
  );
};

export { Nav };
