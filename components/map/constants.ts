const RESTRICTION = {
  latLngBounds: {
    north: 85,
    south: -85,
    west: -180,
    east: 180,
  },
  strictBounds: true,
};

const DEFAULT_CENTER = {
  lat: 0,
  lng: 0,
};

const DEFAULT_ZOOM = 1;

export { RESTRICTION, DEFAULT_CENTER, DEFAULT_ZOOM };
