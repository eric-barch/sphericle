export enum LocationType {
  Quiz = "Quiz",
  Area = "Area",
  Point = "Point",
}

export enum SearchStatus {
  Searching = "Searching",
  Searched = "Searched",
}

export enum DispatchType {
  Added = "added",
  Changed = "changed",
  Deleted = "deleted",
}
